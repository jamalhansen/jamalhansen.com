repos:
  - repo: local
    hooks:
      - id: blog-validate
        name: blog-validate
        entry: blog-validate check --changed
        language: system
        types: [markdown]
        pass_filenames: false
